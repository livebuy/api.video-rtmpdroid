jdk:
  - openjdk17
before_install:
  - git config --global user.email "chifor.mihai21@gmail.com"
  - git config --global user.name "Mihai Chifor"
  - yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses
